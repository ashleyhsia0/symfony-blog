{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ user.username }}'s Blog</h1>

    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        <div>
            {{ post.body }}
        </div>
    {% endfor %}

{% endblock %}
